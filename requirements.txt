gradio==6.17.3
spaces==0.50.4
requests==2.32.5
pillow==12.0.0
pymupdf==1.26.6
json-repair==0.60.1

# Active HF Space path: ZeroGPU + Transformers running the fine-tuned MiniCPM-V model
# (EXTRACTOR_BACKEND defaults to zerogpu; ZEROGPU_MODEL_ID points at the merged model). All wheels —
# no source builds — so the Space build doesn't time out. (llama.cpp is a separate, undeployed path.)
accelerate
bitsandbytes
torch
torchvision
av
transformers[torch]>=5.7.0

gradio==6.17.3
spaces==0.50.4
requests==2.32.5
pillow==12.0.0
pymupdf==1.26.6
json-repair==0.60.1
# Active HF Space path: run the official OpenBMB GGUF through llama.cpp on ZeroGPU.
# Keep this pinned to a wheel release and use the official prebuilt CUDA wheel index so the
# Space build avoids Git-based source resolution.
--extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu125
llama-cpp-python==0.3.28
